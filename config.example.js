// Copy to config.js for a deployed Supabase project. This file is safe for the
// browser only with the project's anon/publishable key; never use service_role.
window.ORCHIDARIUM_CONFIG = {
  supabaseUrl: 'https://your-project.supabase.co',
  supabaseAnonKey: 'your-anon-or-publishable-key',
};
